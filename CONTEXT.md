# 泠靈小工具（Ling Ling Suite）

樂團譜務用的分譜 PDF 管理工具：把出版社或製譜軟體交出的合併譜拆成分譜、依樂器表排序、套用命名格式後歸檔。

## Language

### 譜

**總譜（Score）**：
一首曲目（或樂章）所有聲部合在一起的樂譜，在群組內以序號 00 歸檔。
_Avoid_：合併譜、全譜

**分譜（Part）**：
單一樂器（或聲部）的樂譜，一個 PDF 檔對應一份分譜。
_Avoid_：聲部譜、單譜

**合併譜（Combined PDF）**：
出版社或製譜軟體交出的原始 PDF，通常一個檔案內連著總譜與多份分譜，是分割的來源。分割永遠不修改或搬動合併譜。
_Avoid_：總分譜、原檔

### 組織

**群組（Group）**：
一首曲目的一個樂章及其所有分譜與總譜；命名變數中的曲名、樂章、作曲家、曲種都定義在群組層級。
_Avoid_：曲目、資料夾

**樂器表（Instrument List）**：
群組內樂器的有序清單，順序即總譜順序，位置決定分譜的序號。
_Avoid_：編制表、聲部表

**專案（Project）**：
一次演出季或一場音樂會的所有群組、命名格式與輸出設定，存成 `.llproj` 檔。
_Avoid_：工作檔、存檔

### 操作

**分割（Split）**：
在合併譜上標記分割點並為每段命名，產生對應的分譜檔案；產生的檔案進入工作區。
_Avoid_：拆分、切割

**重新命名（Rename）**：
依命名格式把群組內的分譜與總譜搬到輸出位置並改名；來源可能在工作區，也可能是使用者匯入的既有檔案，兩者走同一個動作。
_Avoid_：匯出、輸出、歸檔

**復原（Undo）**：
把上一次重新命名、分割或旋轉的檔案系統效果還原。

### 儲存位置

**工作區（Workspace）**：
程式自己管理的目錄，只存放程式產生、尚待重新命名的檔案（目前只有分割輸出）；使用者匯入的檔案永遠不進工作區。每個來源合併譜對應工作區內一個子資料夾；子資料夾以來源合併譜為鍵，不分專案。
_Avoid_：暫存區、暫存檔、快取

**輸出位置（Output Location）**：
重新命名後檔案的最終落點，由使用者在預覽時指定。
_Avoid_：目的地資料夾、匯出資料夾
